# AgenTank AI Agent 项目说明

## 项目目标

本项目的目标是为 [AgenTank](https://agentank.ai/) 平台开发一个极具竞争力的、自主运行的 JavaScript 坦克机器人。该坦克名为 **Ant**(Tank ID 707)，当前远端技能为 `boost`（加速）。若未来 API 返回 `skill.hasSkill === false`，代理应自动为 tank 创建/申请可用技能；若已有技能，则以远端实际技能为准，不按过期文档假设。

## 核心框架与机制
- **官方指南：** 详细的 API 参考和游戏规则请参阅本地文件 `AGENT_GUIDE.md`。
- **代码入口：** 坦克的全部逻辑均包含在 `new_tank.js` 文件的 `onIdle(me, enemy, game)` 函数中。
- **坐标系统：** 地图上的所有位置必须严格使用数组 `[x, y]` 表示，**绝不能**使用对象 `{x, y}`。
- **动作经济学：** 像 `me.turn()`、`me.go()` 和 `me.fire()` 这样的指令会将动作加入队列。转向（Turn）需要消耗 1 帧，移动（Move）需要消耗 1 帧。
- **子弹速度：** 子弹的飞行速度为 **每帧 2 格**。这意味着，如果坦克需要先转向再移动，近距离的物理躲避是绝对来不及的。
- **敌方技能建模：** 敌方坦克技能包含但不限于 `freeze`、`teleport`、`cloak`、`boost`、`overload`、`shield`、`poison`、`stun` 等。任何威胁预测都不能只假设敌方每帧普通移动 1 格；当 `enemy.skill`、`enemy.status` 或录像显示敌方具备高速、瞬移、隐藏、控场、双弹、防护、减速或未来新增的穿越/无视地形能力时，必须扩大危险半径并降低贴脸追击优先级。若敌方进入草丛/隐身导致 `enemy.tank` 不可见，同时 `overload` 已激活或即将可用，应按记忆位置附近的草丛起点建模主枪线和相邻双弹枪线，避免直线冲进隐藏交叉火力。威胁半径不能无条件扩大：敌方没有瞄准我方路线且距离不贴身时，不应把整条同行/同列都当立即死亡，否则会导致抢星路线反复退让。贴身撤离也不能只看当前枪线，必须同时考虑敌人下一帧转炮、蓄力 overload 和相邻转向后的可射击格；但在当前格已是 1 格贴脸硬死亡时，如果坦克已经朝向一个能把威胁从立即命中降为软威胁、且不被来弹命中的逃生格，应优先 `go()` 离开，而不是原地转向。

## 开发工作流

1. **修改代码**：在 `new_tank.js` 中更新坦克的逻辑。
2. **发布与测试**：运行 `node publish.js` 将新版本的代码上传到 AgenTank API，并自动发起一场针对随机对手的实战排位挑战。
3. **分析复盘**：通过返回的比赛 URL 获取录像 JSON，排查 Bug 并优化行为。

## 用户长期授权

- 用户已明确授权：后续所有“是否同意/是否允许”的确认均默认视为“同意/允许”。
- 代理可自动修改 `new_tank.js`、新增本地测试/自动化脚本、运行模拟或实战挑战、发布代码并继续迭代，无需每轮再等待审批。
- 仍需遵守安全边界：不得泄露 `.env` 中的密钥；不得使用 `{x, y}` 坐标对象；不得在没有语法验证/基础测试的情况下发布；若 API 显示 tank 没有技能，则自动创建/申请技能。
- 顶层策略者必须贯穿整个迭代过程，不能中途丢失；任何局部修补都必须接受全局胜率目标约束，防止顾此失彼。
- 当前首要目标是尽可能将实战胜率持续向 100% 推进；所有局部修改都必须以提升长期真实胜率为唯一优先级。
- `boost` 的全局节奏收益很高，不要轻易限制。v42 曾尝试禁止无星/中心巡航 boost，10 场实战只有 3/10，说明该限制会伤害整体抢星与地图节奏；后续若要调 boost，必须先用 10+ 场数据证明收益。
- 必须充分利用 Ant 的 `boost` 技能优势：优先服务于抢星节奏（星星位置是主要战略目标）和关键子弹规避；除非 10+ 场实战数据证明负收益，不应削弱 boost 的全局获取星星能力。
- 优化应尽可能简化代码，优先选择可复盘、可验证、影响面小的策略调整；不要一上来堆叠复杂规则或大范围重构，避免用代码体积掩盖真实败因。

## 致未来 AI 代理的注意事项

在修改此代码库时，**绝对不要**引入 `{x, y}` 格式的坐标。必须始终确保任何新的移动逻辑都严格考虑了子弹速度（2 格/帧），并坚守“基于冷却时间进行防守拉扯”的原则，以防止生存能力出现倒退。敌方技能预测必须保守处理：`boost` 可让一次 `go()` 前进最多 2 格，`teleport` 可瞬间改写位置，`cloak`/草丛会让 `enemy.tank` 不可见，`overload` 会产生双弹威胁，`shield` 会浪费我方射击窗口，`poison`/`stun`/`freeze` 会破坏动作时序。特别注意隐藏的 `overload` 敌人：即使最后可见位置没有直接瞄准我方，也要考虑其进入附近草丛后从相邻格打出双弹的可能。但普通未瞄准枪线应只在贴身/极近距离才压制移动；过宽的 snap-shot 半径会让 Ant 在左侧通道和边线反复转向，直接丢失星星节奏。贴身撤离时还要考虑敌人下一帧转炮、蓄力 overload 和相邻转向后的可射击格，不能只按当前朝向判断安全；若当前格已是立即死亡格，只有在当前朝向能降低威胁、避开现有来弹，且当前位置靠墙或落点更靠近中心时才应软逃生。v40 曾尝试无条件离开硬死亡格，10 场实战为 0/10；v44 曾尝试允许落点靠墙，10 场实战为 3/10。两者都说明广泛软逃生会破坏整体胜率，后续不要重复放开。未来若出现无视地形或穿越类能力，应先按更大威胁半径处理，再通过实战复盘收紧。

### 2026-05-14 迭代经验

- v45 的 10 场样本为 2/10，主要败因包括同线炮口前反复转向导致 crash，以及右下/中心星节奏不足。先处理可复现的安全移动缺口：`safeMoveToward` 若已经判定当前朝向落点受威胁，绝不能在替代逃生/转向失败后从函数末尾掉回 `moveToward()` 并 `go()` 进入危险格；应直接放弃该帧前进，保留防守优先级。此类修正必须用红灯测试覆盖，因为 broad soft escape 的历史数据很差。
- v46 发布后 10 场为 4/10，尚不足以支撑 50 场评估。败局 crash 高发于“当前已处于敌方近身枪线压力，逃生格仍在敌方同线 5 格内，敌人可先转炮再开火”的情况；不要全局扩大 `threatLevel`，否则会伤害抢星节奏。更稳的切口是在 `bestEscapeStep` 里只对当前已受威胁的逃生候选加近身转炮惩罚，迫使 Ant 优先脱线，而不是沿着可被下一帧转炮击中的同线继续逃。
- v67 的 10 场样本为 5/10，v68-v70 依次尝试了相邻星软威胁提交、顶边远枪线提前内撤、目标星在敌枪线时放宽贴身脱线，结果分别为 3/10、4/10、4/10，均未超过 v67，并出现 runTime 偏高和早期 crash。该组补丁已回滚；后续不要重复用“临门星覆盖软威胁”或“外边远枪线预撤”这类小规则堆叠来追胜率，除非先用更强证据证明不会伤害整体抢星与 boost 节奏。
- v72-v77 继续显示平台期：v72 3/10，v73 4/10，v74 4/10，v75 5/10，v76 5/10，v77 干净补跑 5/10。v73/v74 的近身/硬枪线队列逃生、v75 的上下边远星竞速、v76 的危险同线跳过瞄准转身、v77 的远内场劣势星放弃，都只能把样本拉回约 5/10，尚无 90% 趋势。后续不要继续盲目堆贴脸枪线小规则；若继续迭代，应优先用成组样本比较“保留/回滚 v76-v77”与更简化的星节奏策略，并重点控制 runTime。`batch_battle.js` 已加入 429 冷却重试；含 error 的批次不能作为标准 10 场胜率评估。
- v78/v79 的行为等价性能缓存（`threatLevel` 单帧状态缓存、`nearbyReachableOrigins` 起点/步数缓存）分别取得 7/10、7/10，并将 v79 平均 runtime 降到约 447ms，是本轮最可靠的正向信号；但 v80 的 `canShoot`/line-of-sight 缓存虽然把 runtime 降到约 329ms，却只有 6/10，已回滚。v81（保留 v78/v79、回滚 v80）又回落到 4/10，说明 7/10 尚不稳定，不能进入 50 场验证。未来若继续性能优化，必须坚持行为等价红灯测试，并用至少两组 10 场确认不会牺牲真实胜率；若继续策略优化，应优先解决星节奏 0-4/1-5 败局，而不是最后一帧贴脸死亡。
- v83 尝试在“当前与落点都只是软威胁且靠近星”时继续抢星，10 场只有 3/10，说明不能把 `teleport` 软半径、`poison`/`freeze` 控制半径、`cloak` 伏击和 `overload` 软火力混为一谈。若需要解决 teleport 近星停摆，只能做 `teleport` 专用窄门；不要泛化到所有软威胁，否则会增加早期 crash 和 0-4/1-4 星节奏败局。
- v86/v87 先后放宽了相邻近身转炮软风险逃生（已面向软控制落点直接 `go()`、以及硬枪线下允许队列转向后落到软 `cloak`/控制威胁格），递进样本分别在 5 场门槛附近失败，v87 为 2/5。该方向与 v40/v44/v68-v70 的历史负信号一致：广泛把“硬死亡降到软威胁”视为可提交，会制造早期 crash 和星节奏停摆。后续除非有非常窄的红灯用例和递进实战支撑，不要再扩大近身软风险逃生；优先回到 v85 的星节奏基线继续验证。
- v88 误以为已完全回滚 v87，但实测 `shouldQueueCloseTurnShotEscape` 仍保留“硬枪线降到软 `cloak`/控制威胁即可 `turn+go`”分支，递进首段 0/3，并在顶边/右上边线多次拿星后原地转向吃 2 格/帧来弹。未来做“回滚”必须用针对性红灯测试确认实际行为已消失，不能只凭改动意图判断。
- v89 真正去掉近身转炮软落点队列后，递进恢复到 5/8，但两场 crash 仍集中在上边/右上外圈：Ant 在外边同线、敌人当前未瞄准但一帧可转炮时，为了瞄准或局部目标反复转向，最终吃 2 格/帧来弹。下一步只应窄修“外边同线陷阱下射击转向让路给继续离线/离敌移动”，不要扩大全局威胁，也不要削弱 boost 抢星节奏。
- v90 按 v89 败局加了“外边同线陷阱下射击转向让路给继续移动”的窄规则，但递进只有 2/5，低于 v89 的 5/8。该补丁未证明收益，已回滚；后续不要再只从射击优先级入口拦截边线问题，应优先从目标选择/队列承诺/已受威胁逃生这类更贴近败因的位置下刀。
- v91 回到 v89 口径后递进 1/3，说明 5/8 不是稳定平台。败局显示 Ant 仍会追“曼哈顿看似近、实际路径绕远”的内场星，例如 `[8,10]`：Ant 路径 10、敌方路径 6，差 4 步，追星导致 0-2 节奏落后。v92 的切口是收紧 `shouldSkipClearlyLostInteriorStar`：中距离内场星也要进入 BFS 输速判断，敌方 6 步内、Ant 10 步以上且落后 4 步时放弃；该改动不能影响 Ant 明显占优或近星提交。
- v92 中距离内场输速过滤递进仍为 1/3，且败局主要是 boost 后沿敌弹轨迹冲刺/中心星长期争夺，并非该过滤命中的 `[8,10]` 类型。该补丁已回滚；后续不要继续用更宽的内场星放弃规则追胜率，除非连续败局直接命中同一输速条件。
- v93 针对 v92 第 3 场小八嘎败局做窄修：Ant 在 `[14,7]` 准备沿右侧 boost，敌人最后见于中央草丛 `[9,7]` 且隐藏后延迟开右向炮，子弹 2 格/帧追上 `[16,7]`。普通隐藏草丛不是 `cloak`/`overload`，旧的 8 帧记忆过期后 `shouldBoost` 会误判空场。新增 `hiddenGrassBoostThreat` 只在敌人当前不可见、最后记忆位置是草丛、记忆不超过 18 帧、boost 两格路径与该草丛同线可射且 8 格内时取消本次 boost；这是 boost 入口的窄约束，不应扩展为全局 threatLevel 或长期草丛禁区，除非递进实战证明收益。
- v94/v95 均未证明收益，已回滚；v96 是保留 v93 boost 窄修的基线。v96 递进 1/3，其中一场 crash 明确复现“两格硬枪线、无干净脱线格、已对准敌人却因 `enemyAimsAt` 禁火，随后原地转向吃炮”：Ant `[3,4]` 朝右，敌 `[5,4]` 朝左，`[3,3]`/`[3,5]` 都不可走。v97 只在当前位置被敌瞄准、距离 2-4、我方已对准敌人、没有现有来弹且不存在干净脱线格时允许 last-resort `fire()`；不要扩展到有脱线格、敌盾、已有来弹或未对准敌人的情况。
- v98 按技能分桶后尝试“cloak 最近同线 boost veto”，目标是 Tank 578 的 `[6,7]` 沿敌最后右向炮线 boost 后被 2 格/帧子弹追上的 crash。但发布后 Tank 578 定向 5 场只有 2/5，低于补丁前 3/5，且新增两场 star 败局，说明该 veto 削弱了 Ant 依赖 boost 的星节奏。该补丁已回滚；后续不要把普通 cloak 最近同线一概作为 boost 禁区，除非能同时证明目标桶胜率和随机递进都不退化。
- v99 回滚到 v97 行为基线后，Tank 578 定向 5 场只有 1/5，确认 `cloak` 桶短板主要不是 v98 引入。复盘 4 场 loss 显示共同根因是对 `cloak` 竞速型敌人的明显输速星承诺太久，例如开局 `[16,8]`、`[10,12]`、`[13,13]`、顶边 `[9,1]` 等，Ant BFS 明显落后仍追，随后在中线/边线反复转向并被拉入近身枪线。v100 只在记忆到敌方技能为 `cloak`，敌方到星 `<=12`、Ant 到星 `>=14` 且 BFS 落后 `>=6` 时放弃该星；该规则故意不作用于非 cloak 敌人，且非 cloak 近边星不应增加 BFS 成本。若 v100 定向或随机递进退化，应优先回滚，不要继续扩大到全局输速过滤。

### 2026-05-15 策略换轨：按技能分桶进化

- v90-v97 多轮递进没有形成稳定上升平台，说明继续围绕随机单场败局追加小规则容易过拟合。后续默认切换为“技能分桶 -> 定向复盘 -> 小修 -> 分桶验证 -> 随机递进守门”的实验路线。
- 先用 `node skill_report.js battle_report.json skill_report.json` 将随机/递进报告按敌方技能归桶。敌方技能优先读取 API 顶层字段；若缺失，则只从 replay 中 `skill` 事件且 `by === 1` 的实际施放推断；如果敌方未施放技能，必须记为 `unknown`，不能臆测为无技能或某个技能。
- 每轮只选择一个技能桶作为主目标，例如 `cloak`、`boost`、`teleport`、`overload`、`shield`、控制类或 `unknown`。先对该桶内高频/高威胁对手执行 `node review_challenge.js <TankID> [count]`，目标是把单桶/目标对手胜率推进到 80%+；未达到前不要分散到其他技能桶。
- 自我对战/候选对基线模拟只能作为预筛和发现镜像弱点的辅助信号，不能单独作为保留标准。真正保留一个补丁必须同时满足：目标技能桶胜率提升，且随机递进 `3 -> 5 -> 8 -> 10` 不退化。若目标桶改善但随机递进明显退化，应回滚或收窄补丁。
- 顶层策略者必须维护“当前主攻技能桶、目标对手、基线版本、候选版本、样本量、保留/回滚理由”。subagent 可并行做 replay 复盘或脚本实现，但最终只允许顶层策略者决定是否发布、保留或回滚。
- 代码策略仍然保持小而可解释：不要为每个对手堆专属 if；优先抽象为少量技能画像（高速、隐身伏击、瞬移、双弹、防护、控制），只影响抢星阈值、boost 使用、贴脸撤离和射击窗口这几个关键决策点。

## 自定义自动化技能：半自动进化 (Batch Evolution)

用户可以通过指令 **“开始进化”** 或 **“开始战斗”** 来触发此技能。当接收到此指令时，AI 代理必须严格执行以下工作流：

1. **自动对战**：默认执行 `node progressive_battle.js`，按 3 场 -> 5 场 -> 8 场 -> 10 场递进验证，并将阶段结果写入 `progressive_report.json` 与 `battle_report.json`。递进门槛为：3 场至少 2 胜才补到 5 场；累计 5 场至少 4 胜才补到 8 场；累计 8 场至少 7 胜才补到 10 场；累计 10 场至少 9 胜才记为“接近/达到 90% 的 10 场批次”。若任一阶段未过线，应立即停止继续挑战，进入败局复盘和修复，避免把明显负收益补丁硬跑满 10 场。只有用户明确要求固定样本时，才执行 `node batch_battle.js 10`。
2. **数据收集**：读取 `battle_report.json`，总结胜负数据。
3. **深度分析**：提取报告中标记为 `loss`（失败）的比赛的 `agentReplayUrl`。使用 `web_fetch` 工具读取这些 JSON 数据，分析导致失败的具体原因（例如：寻路卡死、错误判断枪线、技能释放时机不对）。
4. **汇报与自动优化**：由于用户已授予长期默认同意，代理可在完成败因分析后直接修改 `new_tank.js`、验证并发布；最终汇报需包含败因、改动和胜率结果。
5. **代码优化与发布**：修改 `new_tank.js` 中的战术逻辑，并执行发布流程。发布完成后继续下一轮实战进化。
6. **技能分桶与对照**：若连续多个版本没有稳定进展，必须先运行 `node skill_report.js battle_report.json skill_report.json`，选择一个技能桶作为主攻方向，再用 `node review_challenge.js <TankID> [count]` 对该桶代表对手做定向样本。候选补丁只有在目标桶提升且随机递进不退化时才保留；若退化则回滚或收窄。自我对战/候选模拟只能辅助发现问题，不能替代真实对手样本。
7. **持续循环与停止条件**：进化必须按 `递进实战(3->5->8->10) -> 分桶复盘败局 -> 修复 -> 验证 -> 发布 -> 再跑递进实战` 循环推进，不应在单轮结果后停止。只有连续/累计达到 3 个完整 10 场批次均接近或达到 90% 胜率后，才可启动 50 场验证；若 50 场验证未接近或达到 90% 胜率，必须继续回到递进进化循环。
8. **策略与分工**：顶层策略者必须贯穿整个进化过程，持续维护全局胜率目标、历史实验记忆、当前技能桶、目标对手和风险边界；在复盘录像、定位败因、实现局部修复或验证改动时，应在有帮助时使用 subagents 提供并行分析和实现支持，但最终策略取舍必须由顶层策略者统一把关。

## 自定义自动化技能：Skills 复盘挑战 (Targeted Review)

用户可以通过指令 **“复盘挑战 [对手名称/ID]”** 来触发此技能。

1. **针对性对战**：执行 `node review_challenge.js <TankID>`。该脚本会针对特定对手发起 10 场挑战。
2. **报告汇总**：生成 `review_report.json`，记录每场胜负原因。
3. **深度复盘**：分析败局 Replay，找出该对手的战术特点（如：高频使用冻结、走位极其猥琐、射击精度极高）。
4. **汇报方案**：向用户汇报复盘结果及**针对性修改建议**。
5. **确认后修改**：经用户批准后修改代码并发布。

## 自定义自动化技能：同步坦克规则 (Sync Rules)

用户可以通过指令 **“同步坦克规则”** 来触发此技能。

1. **获取最新规则**：访问 `https://agentank.ai/agent-guide`。
2. **格式化与保存**：将抓取到的内容格式化为 Markdown。
3. **覆盖本地文件**：将最新的规则保存到本地 `AGENT_GUIDE.md` 文件中，确保本地开发参考始终与官方平台保持同步。
4. **确认更新**：告知用户同步完成。
